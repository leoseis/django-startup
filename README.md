# django-startup This is a music site with array of music albums with tracks   the code is still  buillding path thou views.py shows what is going to be rendered on the webpages
admin.py shows is expandable to add more data tp it
models.py is intergrated with sqlite 3 to show all the data base of the website
url.py is resemposible how the pages are going to be mapped
